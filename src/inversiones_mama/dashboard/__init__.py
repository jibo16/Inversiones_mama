"""Local Streamlit dashboard — data loaders and Plotly chart helpers.

The Streamlit glue lives in ``scripts/dashboard.py``. This package holds
the reusable, testable pieces: loaders that pull from cached result
files + fresh pipeline runs, and chart builders that return Plotly
``Figure`` objects.
"""
